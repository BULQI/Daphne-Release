<UserControl x:Class="DaphneGui.CellPropertiesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:daph="clr-namespace:Daphne;assembly=Daphne"
             xmlns:uc="clr-namespace:DaphneUserControlLib;assembly=DaphneUserControlLib"
             xmlns:local="clr-namespace:DaphneGui"
             xmlns:shared="http://schemas.actiprosoftware.com/winfx/xaml/shared"
             xmlns:editors="http://schemas.actiprosoftware.com/winfx/xaml/editors"
             Loaded="UserControl_Loaded"
             mc:Ignorable="d"
             x:Name="CellPropertyControl"
             >

    <!-- This user control shows the cell's immediate properties only, i.e., the properties in the ConfigCell class.
         It does not show the details like mols, reactions, genes, div/death drivers, diff_scheme. 
         The user control CellDetailsControl shows these other items.
    -->

    <UserControl.Resources>
        <local:DatabindingDebugConverter x:Key="DebugConverter"/>
        <CollectionViewSource x:Key="diffSchemesListView" />
        <CollectionViewSource x:Key="moleculesListView" 
             Source="{Binding ElementName=CellPropertyControl, Path=Tag.entity_repository.molecules}"/>                 
    </UserControl.Resources>

    <Grid>
        <Grid.Resources>
            <daph:DiffSchemeToDiffNameConverter x:Key="DiffSchemeToDiffNameConv" />
            <daph:TextToColorConverter x:Key="textToColorConv" />
        </Grid.Resources>

        <local:DaphneStackPanel x:Name="panelCellDetails" Orientation="Horizontal">
            <StackPanel Orientation="Vertical">
                <StackPanel Orientation="Horizontal" Width="230" >
                    <TextBlock Text="name" Margin="6,0" HorizontalAlignment="Right" VerticalAlignment="Center" Width="80" />
                    <TextBox Text="{Binding Path=CellName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,2" Width="150" LostFocus="CellTextBox_LostFocus" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Width="230" >
                    <TextBlock Text="radius" Margin="6,0" HorizontalAlignment="Right" VerticalAlignment="Center" Width="80" />
                    <uc:DoublesBox SliderEnabled="False" TextFieldWidth="130" Margin="-5,0" HorizontalAlignment="Left" Number="{Binding Path=CellRadius, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    </uc:DoublesBox>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Width="230" >
                    <TextBlock Text="differentiator" Margin="6,0" Grid.Column="0" Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Center" Width="80" />
                    <TextBox x:Name="txtCellDiff" Width="130" IsEnabled="False" IsReadOnly="True" 
                                     Text="{Binding Path=diff_scheme, Mode=OneWay, Converter={StaticResource DiffSchemeToDiffNameConv}}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Width="220" >

                    <StackPanel.Resources>
                        <local:diffSchemeValueConverter x:Key="diffSchemeValueConverter" />
                    </StackPanel.Resources>

                    <Expander                                            
                            Padding="6" ExpandDirection="Down"
                            IsExpanded="True"
                            Header="Locomotor"                                                  
                            x:Name="LocomotorExpander"
                            Margin="-4,0,0,0" 
                            Canvas.ZIndex="1"     
                            HorizontalAlignment="Left"
                            Width="Auto"
                            Visibility="Visible"
                            >

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                            <TextBlock Text="driver" Margin="6,0" HorizontalAlignment="Right" VerticalAlignment="Center" Width="60"/>
                            <ComboBox x:Name="cbLocomotorDriver1" MinWidth="100"
                                                DisplayMemberPath="Name"
                                                SelectedValue="{Binding Path=locomotor_mol_guid_ref, Mode=TwoWay}"
                                                SelectedValuePath="entity_guid">

                                <ComboBox.ItemsSource>
                                    <CompositeCollection>
                                        <daph:ConfigMolecule Name="None" entity_guid=""/>
                                        <CollectionContainer  Collection="{Binding Source={StaticResource moleculesListView}}" />
                                    </CompositeCollection>
                                </ComboBox.ItemsSource>
                            </ComboBox>

                        </StackPanel>
                    </Expander>

                </StackPanel>
            </StackPanel>

            
            <StackPanel Orientation="Vertical" Margin="4,0">
                    
                <StackPanel Orientation="Horizontal">
                        <TextBlock Text="transduction constant" Margin="6,0" HorizontalAlignment="Right" VerticalAlignment="Center" Width="120" TextWrapping="Wrap"/>
                        <uc:DoublesBox Margin="0,2" TextFieldWidth="95" SliderEnabled="False"
                                Number="{Binding Path=TransductionConstant, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"    />

                </StackPanel>

                <!--<StackPanel Orientation="Horizontal">
                    <TextBlock Text="debug" Margin="6,0" HorizontalAlignment="Right" VerticalAlignment="Center" Width="120"/>
                    <uc:DoublesBox Margin="0,2" Width="100" SliderEnabled="False"
                                Number="{Binding Converter={StaticResource DebugConverter}, ConverterParameter=drag}"    />
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="drag coefficient test" Margin="6,0" HorizontalAlignment="Right" VerticalAlignment="Center" Width="120"/>
                    <TextBox Margin="0,2" Width="100"
                                Text="{Binding Path=DragCoefficient, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"    />
                </StackPanel>-->

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="drag coefficient" Margin="6,0" HorizontalAlignment="Right" VerticalAlignment="Center" Width="120"/>
                    <uc:DoublesBox Margin="0,2" TextFieldWidth="95" SliderEnabled="False"
                                Number="{Binding Path=DragCoefficient, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"    />
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="stochastic force parameter" Margin="6,0" HorizontalAlignment="Right" VerticalAlignment="Center" Width="120" TextWrapping="Wrap"/>
                    <uc:DoublesBox Margin="0,2" TextFieldWidth="95" SliderEnabled="False"
                                Number="{Binding Path=Sigma, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"    />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="6,6">
                    <Button Name="PushCellButton" VerticalAlignment="Top" Height="24" Width="200" Content="Save Cell to Cells Library..." Click="EcsPushCellButton_Click"/>
                </StackPanel>

            </StackPanel>
            <!--</Expander>-->
        </local:DaphneStackPanel>


    </Grid>
</UserControl>
